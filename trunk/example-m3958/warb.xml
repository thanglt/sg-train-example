<!-- 用ant太慢了（大概7分钟），最快的方法还是手工将war目录复制一下 然后将svn和jar查找一下，删除即可。 -->

<project name="firstgwt" basedir="." default="default">

	<target name="default" depends="deploy"></target>

	<target name="deploy">
		<copydir dest="d:/gwtexport" src="war" excludes="**/.svn **./*.jar"></copydir>
	</target>

</project>